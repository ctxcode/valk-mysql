
test "Multi Statement" {
    let con = connect() ! {
      println(E.message)
      assert(false)
      return
    }
    // Setup table
    let sql = #embed("multi.sql")
    con.run(sql) ! { assert(false) return }
    con.run(sql) ! { assert(false) return }
}
