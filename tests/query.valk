
use mysql

test "Query" {
    let con = mysql:Connection.new("127.0.0.1", "root", "root", "valk", 3306, null)
    con.connect() ! {
        println("> Connection failed: " + EMSG)
        assert(false)
    }
    assert(true)

}
